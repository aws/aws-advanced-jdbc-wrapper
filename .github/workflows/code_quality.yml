name: Qodana
on:
  workflow_dispatch:
  push:
    branches:
      - main
      - 'releases/*'
  pull_request:
    branches:
      - '*'
    paths-ignore:
      - '**/release_draft.yml'
      - '**/maven*.yml'

jobs:
  qodana:
    runs-on: ubuntu-latest
    steps:
      - name: 'Get changed files'
        uses: tj-actions/changed-files@v37
        id: changed-files-specific
        with:
          files_yaml: |
            doc:
              - '**/*.md'
              - '**/docs/**'
              - '**/*.jpg'
              - '**/*.png'
      - name: 'Check changed files'
        id: changed-files
        if: ${{steps.changed-files-specific.outputs.doc_only_changed == 'true' || steps.changed-files-specific.outputs.doc_only_modified == 'true'}}
        run: echo "ONLY_DOCS=false" >> $GITHUB_OUTPUT
      - uses: actions/checkout@v3
        if: ${{steps.changed-files.outputs.ONLY_DOCS && steps.changed-files.outputs.ONLY_DOCS == 'false'}}
        with:
          fetch-depth: 0
      - name: 'Qodana Scan'
        if: ${{steps.changed-files.outputs.ONLY_DOCS && steps.changed-files.outputs.ONLY_DOCS == 'false'}}
        uses: JetBrains/qodana-action@v2022.2.1
