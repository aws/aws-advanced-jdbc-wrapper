# EncryptingPreparedStatement Method Ordering Issues

## Summary
The file has 10 checkstyle violations where overloaded methods are not placed next to each other. This document provides a guide for fixing these issues.

## Violations and Required Fixes

### 1. setDate() overloads (Line 562 should be near Line 395)
**Current locations:**
- Line 395: `setDate(int parameterIndex, Date x)`
- Line 562: `setDate(int parameterIndex, Date x, Calendar cal)` ❌ TOO FAR

**Fix:** Move line 562 method to immediately after line 395

---

### 2. setTime() overloads (Line 572 should be near Line 405)
**Current locations:**
- Line 405: `setTime(int parameterIndex, Time x)`
- Line 572: `setTime(int parameterIndex, Time x, Calendar cal)` ❌ TOO FAR

**Fix:** Move line 572 method to immediately after line 405

---

### 3. setTimestamp() overloads (Line 582 should be near Line 415)
**Current locations:**
- Line 415: `setTimestamp(int parameterIndex, Timestamp x)`
- Line 582: `setTimestamp(int parameterIndex, Timestamp x, Calendar cal)` ❌ TOO FAR

**Fix:** Move line 582 method to immediately after line 415

---

### 4. setBlob() overloads (Line 684 should be near Line 647)
**Current locations:**
- Line 526: `setBlob(int parameterIndex, Blob x)`
- Line 531: `setBlob(int parameterIndex, InputStream inputStream, long length)`
- Line 537: `setBlob(int parameterIndex, InputStream inputStream)`
- Line 684: Another setBlob overload ❌ TOO FAR

**Fix:** Group all setBlob() overloads together starting at line 526

---

### 5. setClob() overloads (Line 689 should be near Line 652)
**Current locations:**
- Line 542: `setClob(int parameterIndex, Clob x)`
- Line 547: `setClob(int parameterIndex, Reader reader, long length)`
- Line 552: `setClob(int parameterIndex, Reader reader)`
- Line 689: Another setClob overload ❌ TOO FAR

**Fix:** Group all setClob() overloads together starting at line 542

---

### 6. setNClob() overloads (Line 754 should be near Line 657)
**Current locations:**
- Line 626: `setNClob(int parameterIndex, NClob value)`
- Line 631: `setNClob(int parameterIndex, Reader reader, long length)`
- Line 636: `setNClob(int parameterIndex, Reader reader)`
- Line 754: Another setNClob overload ❌ TOO FAR

**Fix:** Group all setNClob() overloads together starting at line 626

---

### 7. execute() overloads (Line 804 should be near Line 662)
**Current locations:**
- Line 658: `execute()`
- Line 755: `execute(String sql)`
- Line 804: Another execute overload ❌ TOO FAR
- Line 850: `execute(String sql, int autoGeneratedKeys)`
- Line 855: `execute(String sql, int[] columnIndexes)`
- Line 860: `execute(String sql, String[] columnNames)`

**Fix:** Group all execute() overloads together starting at line 658

---

### 8. getMoreResults() overloads (Line 824 should be near Line 769)
**Current locations:**
- Line 770: `getMoreResults()`
- Line 825: `getMoreResults(int current)` ❌ TOO FAR

**Fix:** Move line 825 method to immediately after line 770

---

### 9. executeUpdate() overloads (Line 834 should be near Line 689)
**Current locations:**
- Line 653: `executeUpdate()`
- Line 690: `executeUpdate(String sql)`
- Line 835: `executeUpdate(String sql, int autoGeneratedKeys)` ❌ TOO FAR
- Line 840: `executeUpdate(String sql, int[] columnIndexes)`
- Line 845: `executeUpdate(String sql, String[] columnNames)`

**Fix:** Group all executeUpdate() overloads together starting at line 653

---

### 10. Additional execute() overload (Line 849 should be near Line 754)
**Current locations:**
- Multiple execute() methods scattered
- Line 850: `execute(String sql, int autoGeneratedKeys)` ❌ NOT WITH OTHER OVERLOADS

**Fix:** Group with other execute() overloads (see #7 above)

---

## Recommended Approach

1. **Group setter methods** (lines 291-641):
   - Keep all setXXX() methods together
   - Place overloaded versions immediately after each other
   - Order: setString, setInt, setLong, setBytes, setDouble, setFloat, setBoolean, setShort, setByte, setBigDecimal
   - Then: setDate (both overloads), setTime (both overloads), setTimestamp (both overloads)
   - Then: setObject (all overloads), setNull (both overloads)
   - Then: stream setters (setBinaryStream, setAsciiStream, setCharacterStream - all overloads)
   - Then: setURL, setRef, setBlob (all overloads), setClob (all overloads), setArray
   - Then: setUnicodeStream, setRowId, setNString, setNCharacterStream (all overloads), setNClob (all overloads), setSQLXML

2. **Group execution methods** (lines 648-668):
   - executeQuery() - both overloads together
   - executeUpdate() - all overloads together
   - execute() - all overloads together
   - executeBatch()
   - addBatch() - both overloads together

3. **Group getter/configuration methods** (lines 673-905):
   - Keep metadata methods together
   - Keep configuration methods together
   - Place overloaded versions immediately after each other

## Estimated Effort
- Time: 30-45 minutes
- Risk: Low (methods are independent)
- Testing: Compile check + existing unit tests should pass

## Notes
- This is a mechanical refactoring that doesn't change functionality
- All methods are public interface implementations
- No logic changes required, only reordering
- Consider using IDE refactoring tools to minimize errors
